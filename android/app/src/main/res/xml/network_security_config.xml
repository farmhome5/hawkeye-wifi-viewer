<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
  <!-- Allow cleartext to any host on the camera AP (testing/dev).
       We'll tighten this later if you want. -->
  <base-config cleartextTrafficPermitted="true" />
</network-security-config>
